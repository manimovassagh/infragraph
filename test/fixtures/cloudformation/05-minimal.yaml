AWSTemplateFormatVersion: '2010-09-09'
Description: Minimal template with a single S3 bucket

Resources:
  MyBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: my-simple-bucket
      Tags:
        - Key: Name
          Value: my-bucket
