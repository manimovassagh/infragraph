# Docker Compose environment — copy to .env and fill in values
# Leave empty for guest mode (no auth, no session history)

# Backend (runtime)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Frontend (build-time — baked into JS bundle by Vite)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
